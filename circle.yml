machine:
  services:
    - docker
  environment:
    DOCKERIMAGE: ericdill/obvious-ci

dependencies:
  # Note, we used to use the naive caching of docker images, but found that it was quicker
  # just to pull each time. #rollondockercaching
  override:
      - docker pull ${DOCKERIMAGE}
#    - docker pull pelson/conda32_obvious_ci

test:
  override:
    # Run, test and (if we have a BINSTAR_TOKEN) upload the distributions.
    - export CONDA_PY=27; export CONDA_NPY=19; ./ci_support/run_docker_build.sh
    - export CONDA_PY=34; export CONDA_NPY=19; ./ci_support/run_docker_build.sh

